<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="demo.image.mapper.ImageRemarkMapper">
  <resultMap id="BaseResultMap" type="demo.image.pojo.ImageRemark">
    <result column="image_id" jdbcType="INTEGER" property="imageId" />
    <result column="remark" jdbcType="VARCHAR" property="remark" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="md5_mark" jdbcType="CHAR" property="md5Mark" />
  </resultMap>
  <insert id="insert" parameterType="demo.image.pojo.ImageRemark">
    insert into image_remark (image_id, remark, create_time, 
      md5_mark)
    values (#{imageId,jdbcType=INTEGER}, #{remark,jdbcType=VARCHAR}, #{createTime,jdbcType=TIMESTAMP}, 
      #{md5Mark,jdbcType=CHAR})
  </insert>
  <insert id="insertSelective" parameterType="demo.image.pojo.ImageRemark">
    insert into image_remark
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="imageId != null">
        image_id,
      </if>
      <if test="remark != null">
        remark,
      </if>
      <if test="createTime != null">
        create_time,
      </if>
      <if test="md5Mark != null">
        md5_mark,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="imageId != null">
        #{imageId,jdbcType=INTEGER},
      </if>
      <if test="remark != null">
        #{remark,jdbcType=VARCHAR},
      </if>
      <if test="createTime != null">
        #{createTime,jdbcType=TIMESTAMP},
      </if>
      <if test="md5Mark != null">
        #{md5Mark,jdbcType=CHAR},
      </if>
    </trim>
  </insert>

  <insert id="batchInsert">
    insert into 
      image_remark(remark, md5_mark)
    values
      <foreach item="imageCache" index="index" collection="imageCaches"
        open="" separator="," close="">
        (#{imageCache.remark}, #{imageCache.md5Mark})
      </foreach>
    ON DUPLICATE KEY UPDATE
      remark = concat(remark, ',', #{imageCache.remark})
  </insert>
  
  <insert id="insertOrUpdate">
    insert into 
      image_remark(remark, md5_mark)
    values
      (#{imageCache.remark}, #{imageCache.md5Mark})
    ON DUPLICATE KEY UPDATE
      remark = concat(remark, ',', #{imageCache.remark})
  </insert>

  <update id="fillImageId">
    <foreach item="imageStore" index="index" collection="list"
      open="" separator="" close="">
      update 
        image_remark 
      set
        image_id = #{imageStore.imageId}
      where
        md5_mark = #{imageStore.md5Mark}
      ;
    </foreach>
  </update>

  <select id="findImageRemarkByEmptyImageId" resultType="String">
    select
      md5_mark
    from
      image_remark
    where
      image_id is null
  </select>

</mapper>