习题页面, 定时发送 GET 保持 online

需要引入 学生-父母关系 部分统计只允许父母 / 管理员查看
练习换积分

习题 按做题时间影响积分计算随机空间
按时间 * 正确率 考量进步空间 影响积分计算
高年级可以做低年级/往学期题目 顺应降低积分空间
设定每日获取积分上限量? 超出后降低随机空间
每日每科首份问卷 固定某个分数, 若首份时间与低分时间冲突, 优先考虑首份
每天做题次数限制?


考虑引入超短时间的识别

考虑引入囤分兑换更高奖励

积分以盲盒表现 按时段划分随机空间
晚间 凌晨 上课时间 采取0分模拟
假期另计

考虑后期引入统计数据
日 周 月 统计

加入习题回顾?



articleLong
	之前保存文件路径异常, 需要修改数据库 & 迁移对应文件

VisitDataServiceImpl
	需要兼容 IPv6

需要重新打理
	user admin 的一堆历史遗留 url

权限模块  重构
	按目前情况精简
		保留 user - role ?
		是否提供编辑 ?
		
	调整 初次启动服务时, 初始化 机构 / 超管 / 权限
	初次登录系统时 强制跳转超管重置密码页面? 创建超管? 
	
Swagger 与 Gson 冲突 待处理

图片信息传送

ocr
	linux 拉取依赖异常, 尝试更新版本号


评论模块
	需要为管理员增加屏蔽用户/ip等手段
		请参照 ipRecord 相关模块
			
图床
	暂时未有合适的 webp webm 支持库
	上传 webp / webm 时, 会返回异常.
	等有成熟转换技术再作打算
	因有保存截屏类数据进数据库, 需要删除过时数据  --- 2020/02/29	


cloudinary 可用图片url 上传图片
https://cloudinary.com/documentation/image_upload_api_reference

CREATE TABLE `duralbe_recorder` (
id bigint not null,
   `trading_id` bigint default null,
   `holder_id` bigint NOT NULL,
   `amount` decimal(9,2) NOT NULL,
   `transation_date` date NOT NULL,
   `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
   
   `remark` varchar(64) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci DEFAULT NULL,
   is_broken boolean default false,
   
   is_delete boolean default false,
   PRIMARY KEY (`id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='duralbe_recorder'
;

-----------------


验证码模块

maxAttempts user_attempts 增加时间列?
-------------

books
3小时读懂现代中国